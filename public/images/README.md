# Portfolio images

- Project images are loaded from Unsplash in `src/components/Projects.tsx`. You can replace them with your own image URLs or add local images here and reference them as `/images/your-file.jpg`.
